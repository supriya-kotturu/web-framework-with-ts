{
	"name": "web-framework-with-typescript",
	"version": "0.0.0",
	"scripts": {
		"dev-run": "vite --port=3001",
		"dev-db": "json-server -w db.json",
		"dev": "concurrently \"npm run dev-db\" \"npm run dev-run\"",
		"build": "tsc && vite build",
		"preview": "vite preview"
	},
	"devDependencies": {
		"typescript": "^4.4.4",
		"vite": "^2.7.2"
	},
	"dependencies": {
		"axios": "^0.26.0",
		"concurrently": "^7.0.0"
	}
}
